# 安全报告

请通过 [GitHub 私密漏洞报告](https://github.com/Kamisato-Yuna/MLX-Gateway/security/advisories/new) 报告可能涉及凭据、私有路径、请求数据或进程控制的问题，不要在公开 Issue 中粘贴敏感日志。

项目目前处于早期开发阶段，安全修复优先针对 `main`。报告请包含受影响提交/版本、复现步骤、影响以及已脱敏的证据。

## 运行边界

- 默认仅监听回环地址。网关与 MLX 后端均没有应用级认证，不适合直接暴露到公网或不受信任的局域网。
- API 不能启动或切换模型。只管理本应用启动的子进程，停止时取消在途请求。
- 仅从用户指定目录读取模型，后端使用 Hugging Face 离线模式。应用不会下载权重。
- 公共模型列表与网关错误不应暴露本机绝对模型路径。完整日志保留在本机；分享前请脱敏。
- 不要提交 `.env`、密钥、证书私钥、模型、缓存或运行日志。

源码开源不代表已经完成应用签名、公证、第三方审计或生产部署验证。
